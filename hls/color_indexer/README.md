# Color Indexer
Indexes incoming AXI streaming color data into horizontal or vertical line of groups. Grouping is based on register settings accessible by an AXI-lite bus

There are two components: one for horizontal grouping and one for vertical grouping. The source code is the same between them, with a macro to control the logical differences
